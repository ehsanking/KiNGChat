services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: elahe-app
    image: elahe-app
    restart: unless-stopped
    environment:
      # Database — PostgreSQL via docker network
      - DATABASE_URL=${DATABASE_URL:-postgresql://${POSTGRES_USER:-elahe_u7f3d9a1}:${POSTGRES_PASSWORD:-R9m4K2p8V6x1Q3t7N5w0B4c2H8y6L1z9}@db:5432/${POSTGRES_DB:-elahe}}
      - PRISMA_CONNECTION_LIMIT=${PRISMA_CONNECTION_LIMIT:-10}
      # Storage
      # File storage now defaults to the local filesystem.  To enable an
      # external object store (e.g. MinIO or S3) set the corresponding
      # environment variables (MINIO_ENDPOINT, MINIO_ACCESS_KEY,
      # MINIO_SECRET_KEY) here and implement a storage adapter.
      # Redis (optional — leave empty to disable)
      - REDIS_URL=${REDIS_URL:-}
      # App
      - APP_URL=${APP_URL:-http://localhost:3000}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      # Security — defaults provided so server starts even without .env
      - JWT_SECRET=${JWT_SECRET:-changeme_jwt_secret_min32chars_xxx}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY:-changeme_encryption_key_32chars!}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
      # VAPID (optional — push notifications disabled if not set)
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY:-}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY:-}
      - VAPID_EMAIL=${VAPID_EMAIL:-}
      # Firebase (optional)
      - NEXT_PUBLIC_FIREBASE_API_KEY=${NEXT_PUBLIC_FIREBASE_API_KEY:-}
      - NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN:-}
      - NEXT_PUBLIC_FIREBASE_PROJECT_ID=${NEXT_PUBLIC_FIREBASE_PROJECT_ID:-}
      - NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET:-}
      - NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID:-}
      - NEXT_PUBLIC_FIREBASE_APP_ID=${NEXT_PUBLIC_FIREBASE_APP_ID:-}
      - NEXT_PUBLIC_FIREBASE_VAPID_KEY=${NEXT_PUBLIC_FIREBASE_VAPID_KEY:-}
      # Rate limiting
      - RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-900000}
      - RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS:-100}
      - SOCKET_RATE_LIMIT_WINDOW_MS=${SOCKET_RATE_LIMIT_WINDOW_MS:-10000}
      - SOCKET_RATE_LIMIT_MAX=${SOCKET_RATE_LIMIT_MAX:-30}
      # Queue
      - QUEUE_CONCURRENCY=${QUEUE_CONCURRENCY:-5}
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 12
      start_period: 180s
    networks:
      - elahe-net

  caddy:
    image: caddy:2-alpine
    container_name: elahe-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"    # HTTP/3 (QUIC)
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      app:
        condition: service_healthy
    networks:
      - elahe-net

  db:
    image: postgres:16-alpine
    container_name: elahe-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-elahe_u7f3d9a1}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-R9m4K2p8V6x1Q3t7N5w0B4c2H8y6L1z9}
      - POSTGRES_DB=${POSTGRES_DB:-elahe}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-elahe_u7f3d9a1} -d ${POSTGRES_DB:-elahe}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 20s
    networks:
      - elahe-net

  # The MinIO service has been removed in favour of local storage.  If you
  # wish to use object storage, configure an external MinIO or S3 service
  # and set the appropriate environment variables in the app container.

networks:
  elahe-net:
    driver: bridge

volumes:
  pgdata:
  caddy_data:
  caddy_config:
