"use client";

/**
 * ContactSidebar is responsible for rendering the list of contacts and groups
 * available to the user.  By extracting this into its own file the chat
 * dashboard can be decomposed into smaller, easier to test pieces.  The
 * current implementation in page.tsx should be migrated here in a future
 * refactor.
 */
export default function ContactSidebar() {
  return null;
}
