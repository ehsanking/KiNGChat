"use client";

/**
 * AdminDrawer will house the controls and views for administrators.  Splitting
 * this into its own file encourages a clear separation between end user
 * functionality and privileged administrative operations.  Populate this
 * component with the admin UI elements currently in page.tsx.
 */
export default function AdminDrawer() {
  return null;
}
