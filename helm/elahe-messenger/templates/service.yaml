apiVersion: v1
kind: Service
metadata:
  name: {{ include "elahe-messenger.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "elahe-messenger.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
