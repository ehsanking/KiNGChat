"use client";

/**
 * MessageComposer encapsulates the input area and controls used to compose
 * messages, including attachments and typing indicators.  Breaking this
 * functionality out of page.tsx simplifies the chat dashboard and makes the
 * message composition logic easier to reason about.
 */
export default function MessageComposer() {
  return null;
}
