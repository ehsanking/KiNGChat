apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elahe-messenger.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['{{ include "elahe-messenger.fullname" . }}:{{ .Values.service.port }}/api/health/live', '-O', '-']
  restartPolicy: Never
